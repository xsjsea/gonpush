<%= form_with(model: area, local: true) do |form| %>
  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :areaName %>
    <%= form.text_field :areaName %>
  </div>

  <div class="field">
    <%= form.label :area_level %>
    <%= form.number_field :area_level %>
  </div>

  <div class="field">
    <%= form.label :area_sequence %>
    <%= form.number_field :area_sequence %>
  </div>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
