<%= form_with(model: social_channel, local: true) do |form| %>
  <% if social_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(social_channel.errors.count, "error") %> prohibited this social_channel from being saved:</h2>

      <ul>
      <% social_channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
