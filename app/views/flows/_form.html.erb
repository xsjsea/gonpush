<%= form_with(model: flow, local: true) do |form| %>
  <% if flow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flow.errors.count, "error") %> prohibited this flow from being saved:</h2>

      <ul>
      <% flow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flow_type %>
    <%= form.text_field :flow_type %>
  </div>

  <div class="field">
    <%= form.label :flow_name %>
    <%= form.text_field :flow_name %>
  </div>

  <div class="field">
    <%= form.label :flow_order %>
    <%= form.number_field :flow_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
